# Data availability

This repository documents a public-data spatial analysis. Raw and intermediate datasets are **not redistributed** here because the project combines multiple third-party public-data sources with their own update cycles and usage terms.

A **public-safe technical excerpt** and selected figures are provided under `outputs/` and `assets/` to document the analytical reasoning without presenting the working datasets as redistributable source data.
